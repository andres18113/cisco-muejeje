# Server-PT — diagnostic closeout and product-readiness implementation
Status: proposed next work order. Offline implementation only after the operator selects this task. This file is not a LIVE, merge, or capability-promotion grant.

## Outcome and baseline

Preserve the accepted diagnostic evidence, finish the missing closeout evidence as an additive package, and deliver a tested production HTTP forwarding prerequisite plus a bounded DHCP native-default transition design. Do not repeat D-DHCP or D-WEB to repair documentation. Diagnostic completion remains separate from product acceptance.

Repository: andres18113/cisco-muejeje.
Reviewed delivery: 52969849408d195436ae250f611318e7e959876f.
Tree: 62f569e5311771e378a54f5b7aeab1d9abe480c7.
Branch: feature/server-pt-goal-foundations.
Observed main: 6263344e31ba3b0de6539d652f2cd06fc73a3562.
CI: 35646191228, six successful jobs.
Campaign ZIP SHA-256: d30eeabc3fbb034e851e9519451fe1d8c196c7151c900e85e52b5af280755c11.

Reconcile the actual checkout before editing; do not reset later work. Use the checkout's instructions and environment. Apply the incremental V-model with a proportional L-risk design delta recorded before behavior. Keep source, observations, inference, authorization, and acceptance separate. Do not rebuild the accepted R1/R3/R4/C1 fixes.

## Evidence to carry forward

D-DHCP attempt 1 at 7660bd1 retained a postflight response and remains stopped. Its successor at 5296984 completed with 41 operations. Six native-default snapshots localize the first difference to the WHOLE configurePcIp interval. Four fields changed: network, mask, start, end. Pool configuration while disabled and process enable introduced no additional default differences. No client acquisition was measured.

D-WEB at 5296984 completed with 63 operations. W1 observed VLAN 1 Fa0/1–3 in LIS. The ping returned one of four packets, with unique source attribution and stable bindings. The HTTP client observed the marker at its first performed inspection, offset 4.359 seconds, and at the late control. The FWD sample belongs to W5 AFTER the fetch, not to a newly observed boundary between ping and fetch. Therefore no statement that "ping caused STP forwarding", no cold-path success, and no product-level cause confirmation follows.

The native default and intended pool share a subnet and overlap in address range. Stored pool coexistence is not proof that the intended pool will serve a client.

## A. Additive evidence closeout

Keep the original ZIP and every attempt file byte-identical. Produce an addendum with its own hash and index, not a rewritten "final" history.

Recover the original three permanent attempt-marker files named in FINAL-MANIFEST.sha256 and verify their bytes against those existing hashes. The delivered ZIP contains their names, sizes, and digests, but not the files. Recover the original post-restart process/mailbox/campaign census and its timestamp. If it was never saved, record that historical observation as not independently verifiable; a new read-only OS census may establish current state only. Do not backdate or recreate missing historical facts.

Retain the operator's actual campaign grant and receiver-risk acceptance, and the later lifecycle permission amendment if available. The D-DHCP-2 envelope says force termination unauthorized; a later retirement record and D-WEB envelope say authorized. This is not evidence that force termination occurred. Preserve the sequence and its authority source instead of editing earlier envelopes or inferring consent from their own fields.

If a source cannot be recovered, state the exact missing claim. This documentary limitation does not stop safe offline work in B/C, and never licenses another experiment. No Packet Tracer launch, contact, termination, command publication, wildcard cleanup, or marker recreation is needed.

## B. Product HTTP: observe forwarding, do not warm the path

Implement one bounded, read-only forwarding prerequisite on the real public service workflow. Reuse the existing exact-port/VLAN STP observation and parser. Do not copy the Voice pipeline, duplicate a parser, introduce a second service subsystem, or add a user switch that silently bypasses readiness.

Inspect the seam through apply_enterprise_services, its E5 closure, foundation derivation, ServiceApplicator, and runtime composition. Derive relevant switch ports and VLANs from the actual manifest/plan, never from fixture names or diagram position. For the supported single-segment topology, require fresh, complete, uniquely attributed FWD evidence for all required access ports before the first HTTP client request. Group observations by switch/VLAN; do not multiply the same query by client count.

Keep operational readiness distinct from configuration application, service eligibility, and behavioral verification. LIS/LRN/BLK, missing/duplicated interfaces, wrong VLAN, stale output, wrong device identity, transport failure, and timeout do not authorize HTTP. Record a precise blocked or inconclusive result with per-client coverage, not "HTTP listener failed". Bound the total wait, samples, and nested calls at the point they execute.

Persist the forwarding evidence and its relationship to the dependent request. A cached prior result cannot replace a fresh prerequisite when identity/state may have changed. Preserve existing retention, optional-service exclusion, uncertainty, ownership, secret, and public JSON guarantees. The four-argument MCP signature remains unchanged. The product tool never deletes the user's topology.

No ping, PortFast, link bounce, clock acceleration, extra HTTP warming request, or arbitrary sleep is a remedy. The future cold HTTP acceptance must be HTTP-only by IP before any DNS verification that itself uses ping. Otherwise DNS validation could warm the same path and conceal the defect.

Write causal tests over the real composition and an independently timed backend: ports report up while STP remains LIS, FWD arrives later, no request precedes FWD, and a request follows the first admissible observation. Include persistent non-FWD, foreign/partial samples, multi-client grouping, record round-trip, and a positive already-FWD control. A timer in the fake must progress independently of query count. Do not invent a failing test if inspection shows an equivalent correct gate already exists; demonstrate and reuse it.

## C. DHCP: design the native lifecycle contract without promoting allocation

Prepare the smallest executable pure assessment and its tests, or a concise design delta if integration changes the existing authorization contract. Compare the initial stock default separately from the post-E5 native state. The measured transition may be admitted only for its exact reviewed model/build/interface/context and complete field relationship; unrelated drift must still refuse. Bind backend-specific policy through composition, not a backend version hardcoded in the domain.

Preserve all snapshots. Never redefine the post-E5 state as if it had existed at baseline. Never delete, rename, rewrite, or hide serverPool. The measurement covers the whole configurePcIp call, not one internal setter. Do not extrapolate its arithmetic to unmeasured networks. Further unexplained changes after the admitted transition remain non-authorizing.

Future acquisition acceptance must identify the exact intended pool, server, client interface and MAC, and a matching freshly observed lease/configuration. A matching IP that could come from the overlapping native default is insufficient. No result here establishes table exhaustion, end-of-table semantics, lease-time freshness, event release, or POP3 behavior.

Do not consume old Q3 slots or modify its historic profile invisibly. Any successor qualification/acceptance requires its own reviewed contract, identity and LIVE grant. Keep product DHCP capabilities UNKNOWN until appropriate qualification; no test-only catalog injection may leak into public composition.

## Permissions, verification and delivery

Use at most two independent offline subagents, no nested fan-out, one writer per worktree and one integrator. No subagent may contact Packet Tracer. Autofix is limited to causal defects inside this task; a material scope, interface, authority or evidence-policy expansion returns a specific decision rather than a speculative fix stack.

Preserve immutable evidence, other worktrees, main, IoT, CP-LIVE and the existing transports. No broad formatting, suppression, or acceptance weakening. Local feature commits are part of the work; publishing requires the operator's applicable grant. No merge, force-push, LIVE, or capability promotion is included.

Run focused regressions, affected integration/system checks, the stable full suite, namespace, documentation, whitespace and exact-delivery quality gates. Obtain exact-SHA CI when publishing is authorized. Report pending evidence honestly instead of repeatedly rerunning unchanged bytes.

Deliver READY_FOR_REVIEW with one current concise record: exact identity, requirement-to-test links, actual changed paths, evidence addendum status, HTTP gate behavior, DHCP transition scope, and remaining product acceptance work. The finish line is offline readiness and auditable closure, not a green label on every service. An unavailable archival source is a named partial item, not a reason to loop indefinitely.
