# Server-PT: evidence-led diagnostic Goal

**Status: READY_FOR_AUTHORIZATION — NOT A LIVE GRANT.**
This replaces the instruction to reimplement the closed residual fixes. It does not replace historical evidence or revive any spent authorization.

## Outcome

Complete the existing D-DHCP and D-WEB diagnostic phase, preserving enough native evidence to identify the first supported boundary for each unresolved problem, or return an explicit evidence-backed blocker. A valid negative is evidence, not a reason to repeat an unchanged experiment. Diagnostic completion is not product acceptance, capability promotion, DHCP acquisition proof, HTTPS listener-isolation proof, or end-to-end mail verification.

## Reviewed starting point

```text
Repository: andres18113/cisco-muejeje
Branch: feature/server-pt-goal-foundations
Reviewed SHA: 0ea738b4326227e9c8030bb906ede13e33d96c65
Reviewed tree: 63b3c4e450eb0dbb1bdd62ad53a57c4271e84836
Observed main: 6263344e31ba3b0de6539d652f2cd06fc73a3562
Exact-SHA CI: 35628775918, all six jobs successful
```
The independent review accepts the bounded offline corrections: per-dispatch local authority checking; exceptional-path terminal observation; the finite closed-window ping schedule; campaign-release evidence; and bounded lifecycle readers. Preserve them. The newest author delta consists of tests and a delivery record, not a fresh implementation of the earlier changes. Inspect any later branch movement; never reset or overwrite it to make the checkout resemble an old prompt.

Read the active checkout's AGENTS.md and engineering standard, the current diagnostic contract, and relevant source/tests. Use historical files only to resolve a specific question. One writer owns each worktree and its own virtual environment. Keep the instruction-loading observation distinct from directly reading files; an unobservable interactive command is not a failed product test.

## The decision still awaiting the operator

A local guard does not fence the receiver inside the native evaluation. A different process could consume a published request after the local observation. The reviewed implementation explicitly retains this limitation; do not relabel it as universal exclusion or an in-band session fence.

The proposed execution model is a disposable, exclusive lab session: no valuable document is open, no competing Packet Tracer instance or campaign writer runs, the operator controls all launch/restart activity, and no automatic restart is allowed during an attempt. The process guard, incarnation checks, campaign claim and object ownership remain mandatory. The operator must explicitly accept this limited failure model before LIVE. An isolated test machine or existing disposable environment is preferred. Do not create a VM/account, change ACLs, stop unrelated programs or migrate user work without permission.

If this environment cannot be established, report that one prerequisite; do not rewrite the working guard repeatedly or try an alternative transport. A stricter requirement for receiver-side atomic exclusion is a separate architecture change with its own approval.

## Proposed campaign — enabled only by an explicit grant

Suggested campaign identity: `SERVER-PT-DIAG-0EA-01`.

| Profile | Channel/build | Hard maximum per attempt | Attempts |
| --- | --- | --- | --- |
| D-DHCP, profile version 3 | file / 9.0.1.0858 | 50 operations, 1500 seconds; preserve 11 operations / 300 seconds for finalization | At most 2 |
| D-WEB, profile version 3 | file / 9.0.1.0858 | 80 operations, 1800 seconds; preserve 10 operations / 300 seconds for finalization | At most 2 |

These are proposed authority limits, not runtime estimates. There are no shared credits between profiles. Q3 3/3 and Q1 2/2 remain exhausted. No Q0, Q1, Q1b, Q2 or Q3 replay is included.

For each permitted attempt, materialize the existing authorization schema using the exact SHA/tree, profile, ordered selected steps, models, ports, nonce, instance identity, observed process incarnation and new attempt identity. Derive the fixture and link list from the reviewed stage definition; do not invent replacements. The initial attempt uses the reviewed SHA. A corrective successor may be proposed only under the autofix rules below, with its own exact identity and CI. Missing or mismatching authority refuses before effects. A draft Markdown file or a locally invented `GRANTED` flag is not user authorization.

Do not run either stage before the operator grants this campaign and accepts the stated receiver limitation. Read-only repository inspection and preparation of these values are allowed beforehand; Packet Tracer launch/contact is not.

## Operation after authorization

Use one LIVE lead. Subagents may handle sizeable independent offline investigations or code/test work in isolated worktrees; they must not open Packet Tracer, publish mailbox requests, change authorizations or touch the lead's working files. Cap concurrent subagents at two and nesting at one. Do not create an agent for every assertion or merely to repeat a completed check.

Begin each attempt in a fresh dedicated Packet Tracer process. A missing process is a launch prerequisite, not a reason to ask the user to perform an already-authorized routine step. Observe executable/build, PID and creation identity, all other process instances, mailbox state and the disposable empty workspace. Stop on an unknown user document or competitor. Never discard unsaved user work.

No restart, channel switch or continuation occurs inside a failed attempt. Preserve its record before any recovery. Terminate only the exactly identified disposable process, gracefully first; force termination is permitted only if the explicit campaign grant includes that lifecycle operation. A new process does not make old requests safe. Before any subsequent launch, preserve and reconcile only the failed campaign's exactly identified mailbox artifacts while no receiver or competing writer is active. Never delete another writer's artifacts or clear the shared directory by wildcard. Unresolved provenance remains a blocker.

Run the existing CLI/coordinator, not direct ad hoc setters. Every backend call uses its ledger. Take authorized terminal observations before fixture deletion when authority and ordinary allowance permit. Preserve cancellation, failed persistence, missing readings, residual engine state and local coordination residue as separate outcomes.

## Scientific and implementation constraints

D-DHCP observes the baseline and the native default around server static configuration, intended-pool configuration while disabled, and process enable. Clients remain inactive. Do not delete, rename, reset or manually rewrite the native default. A coherent default transition is a finding; contradictory configuration, unknown effects or lost identity are stop conditions. Attribute an interval to its actual intervention, not to an unobserved internal setter.

D-WEB keeps exact VLAN/port forwarding evidence, bindings, the one declared ping stimulus, listener state, page marker, client owner/mode/native start result, timed reads, and release evidence. Link-up or a green light alone proves neither forwarding nor HTTP. Ping may change ARP/MAC state and is not passive evidence. A successful diagnostic after a ping does not by itself establish the uninstrumented product path. Timeout is not listener refusal. No unqualified observer, guessed API, new TCP experiment, speculative sleep, PortFast, link bounce or hidden extra request is authorized.

Read the installed Cisco reference for a previously unused member. Documentation establishes a contract candidate, not native support. Record findings against the executed build, channel, model and SHA; do not change the attribution to a later commit.

## Autofix and validation

Autofix is allowed only after a recorded causal defect in the existing instrument or integration prevents the authorized question from being tested. Preserve the failed attempt. Reproduce the defect offline against the relevant real components and independent state/time oracle, then make the smallest in-scope correction. Do not manufacture a RED for documentation or tests-only maintenance and do not reimplement a fix already present.

No acceptance weakening, new protocol/bridge, capability promotion, event-verification expansion, safety-policy change, budget increase, model substitution, claim reset or historical edit is included. Such a change stops for a new decision. An honest inconclusive backend observation is not an autofix target.

Use the repository's required focused, affected and final gates. Run the full local suite once on the stable executable candidate; do not rerun it without a changed cause merely because another model says to double-check. Final delivery still requires exact-SHA quality and CI. Publish only ordinary fast-forwards on the approved feature branch. No merge to main or force push. Reuse valid evidence with its original identity, never relabel it.

Before consuming a second attempt, state briefly the failed boundary, the new evidence, the corrected code/contract location, the unchanged experimental constants and what outcome will discriminate the hypothesis. A native negative does not license an identical retry. Each attempt, including failed or aborted attempts after dispatch, stays in the campaign accounting.

## Completion and communication

One final delivery contains all original records, stdout/stderr/exit codes, hashes, lifecycle and mailbox evidence, source/CI identities, actual operation/time usage, and a concise decision table for BOTH profiles. Use one current projection and an index; do not append another complete history to the brief.

Report separately: `DIAGNOSTIC_PHASE_COMPLETE`, `BLOCKED`, `PRODUCT_NOT_YET_ACCEPTED`, and any residual uncertainty. A complete report alone is not a completed experiment; two completed diagnostics do not mean Server-PT services are production-qualified. Independent acceptance remains external to the authoring agent.

Give a brief update only for a material finding, changed causal direction, risk or genuine blocker. Make routine in-scope decisions without repeated approval requests. If `/goal` is available in the installed interactive Codex, bind this file as the operating contract; otherwise use the same contract as a normal task. CLI help that lacks a `goal` subcommand does not prove whether the interactive slash command exists.
