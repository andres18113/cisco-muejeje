# SERVER-PT-DHCP-AUTONOMOUS-02: episode 10

This immutable archive contains the shifted-policy two-selected-client product
run on Packet Tracer 9.0.1.0858, file channel. Executed source `e8c810192b44d75340ffa6ad81c16473eb060fd2`
(tree `81802ac0307688a5acde8e503b169c39b82af857`), attempt `4d5e9960ce0a4de291697ce7c667c7d2`, instance
`dfb9395b842d4099bab53ba77c8d3d6a`, profile `Q3-NATIVE-PRODUCT` version 3.

The requested unnamed state-only DHCP policy bound to physical `serverPool` on
`192.0.2.0/24`, gateway `192.0.2.1`, DNS/server `192.0.2.10`, capacity two,
window `.151`-`.152`, with `.1` and `.10` excluded. The maintained A1-E6
product record is VERIFIED and sealed. PC1 received `192.0.2.151` with MAC
`00D0.FF28.2034`; PC2 received `192.0.2.152` with MAC `0010.11A4.476E`.
Each result joined two stable fresh samples and a positive exact IP/MAC/port
row in `serverPool`; the group trace is retained once on the triggering client
and referenced by the other. Both first HTTP-by-IP requests to
`http://192.0.2.10/` returned the expected marker without ping, DNS, warm-up,
PortFast, clock acceleration, or client static-address injection.

`M-NATIVE-PRODUCT` and `M-NATIVE-PRODUCT-FINAL` are SUPPORTED_IN_SAMPLE for
this exact source, build, fixture, policy and channel. The qualification used
89 operations and 159.469 active seconds. The closed campaign ledger charged
319.202291 seconds for the whole episode; cumulative use is 608 operations
and 3074.101403 seconds. Fixture restoration was proven. Maintained retirement
exited owned PID 4956; final census showed zero Packet Tracer processes,
empty mailbox and no campaign lock. Explicit `dhcpRun` causality, renewal,
table-end proof, other networks/builds, public default
catalog registration and independent product acceptance are not claimed.

The qualification record SHA-256 is `77d957b092d7c74e021b01f37e5e88837a1efcdf452f71dac6b24d41a9a9e60e` and the product
record SHA-256 is `909e55d1fdeb8bdc6c3c8763c16464c6fb5995b836221ec41b93930cfa7fe63e`. Both originals remain in their indexed
campaign locations. `store/` is the cumulative campaign snapshot after this
episode closed. `MANIFEST.sha256` pins README, lead, record and store bytes;
one-use Python scripts are preserved as `.py.txt` data.
