# SERVER-PT-DHCP-AUTONOMOUS-02: episode 7

This is the immutable primary archive of the second maintained product candidate attempt on Packet Tracer 9.0.1.0858. `MANIFEST.sha256` hashes this README and every file in `lead/`, `record/`, and `store/`. The one-use Python scripts retain their bytes with `.txt` appended. `store/` is the cumulative campaign snapshot after the episode closed. Both records were copied, not moved; the originals stay where the campaign index cites them.

Executed commit `81af939fa6ef34120c16e21e54284e9b9504e753` (tree `6c9aabbf73ab490682bf75f1947679032bdbed57`), attempt `bd94e8f5316748819310a0ef0983c995`, instance `a51a544a525f4d69934c07ad78ccb89b`, file channel. Qualification record `q3-native-product-2026-09-26T03-08-22Z-aad4b6b4.json` has SHA-256 `a8b0ce28b50ba30bcbb34f2d65b672fafc5033b38913598d1f26131f482fe4cc`. Product run record `2026-09-26T03-08-22Z-aad4b6b4-product.json` has SHA-256 `574e52e9d02d17e39b544333dda30a341ea37b47feaeccb9c62d058c79a0a9ee`. Both are registered as campaign external sources (`source-ref-qualification-record`, `source-ref-product-record`).

The exact owned four-device, three-link fixture was created and verified. Product A1-E6 ran E5 (IE-2000 VLAN 10, two access ports, server static address, PC1 DHCP mode) and E6 (native `serverPool` policy and enable). The required `DHCP_LEASE` check for PC1 was VERIFIED at claim level `attributed_to_effective_server_pool`, with fresh evidence and the limitations `autonomous_state_not_explicit_dhcpRun_causality` and `positive_row_does_not_establish_table_end`. The optional `dhcp_lease_attributed` check was unobservable (`lease_client_identity_invalid`). No explicit `dhcpRun`, ping, DNS or static client address was dispatched.

HTTP was withheld and never dispatched. The access-forwarding readiness group for switch `Q3-DEFAULT-ACCESS-SW-01`, VLAN 10, interfaces `FastEthernet1/1` and `FastEthernet1/3` took one complete sample at 19.3 s showing both ports in STP `LIS` with role `Desg` (11 channel calls); the next sample fell after the 30-second group deadline and was refused as `sample_call_budget_exhausted`. The dependent `http_fetch` check is `dependency_blocked` with `access_forwarding_not_ready`, observation `not_attempted`. Product status is partial and `M-NATIVE-PRODUCT` is INCONCLUSIVE. This is a negative observation of access readiness timing, not of native DHCP or of the HTTP server.

The terminal native policy, client and lease inventory was complete (`M-NATIVE-PRODUCT-FINAL` SUPPORTED_IN_SAMPLE) and fixture restoration was proven. The launch was recorded on its first capture. Maintained retirement closed owned PID 40156 on its first attempt with basis `owned_qualification_restored`; the final census found zero Packet Tracer processes, no pending mailbox file and no campaign lock. Episode 7 used 78 qualification operations and closed at 342.694246 seconds. Cumulative use is 338 operations and 2018.808784 seconds, leaving 8,662 ordinary operations and 11,781.191216 ordinary seconds outside protected reserve.
