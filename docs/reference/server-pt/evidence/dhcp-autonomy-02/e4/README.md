# SERVER-PT-DHCP-AUTONOMOUS-02: episode 4

This is the immutable primary archive of the native DHCP policy E5
reapplication experiment on Packet Tracer 9.0.1.0858. `MANIFEST.sha256`
hashes every file in `lead/`, `store/`, `record/`, and this README. The three
one-use lead scripts retain their exact bytes with `.txt` appended to their
original `.py` names. `store/` is the cumulative campaign snapshot after
episode 4 closed, including earlier records named by its index.

The executing commit was `fee6a152f64fb48350127d3c0d8f124bf885ba84`
(tree `3b8b858440acb4c871d4c113bac53d6eb712ba6b`). Episode 4 used
attempt `2242018879574929b400839cc5f60059`, instance token
`9c7bce4bbf1849f0ade10e82b825090e`, and the file channel. The original
record is `q3-native-stability-2026-09-25T22-24-40Z-ab721e25.json`
(SHA-256 `b5ed514137c88e0fd9230bfd36d06e5452109935d15b27988ee30b19853493c8`).

The first launch record was refused as Packet Tracer's main-window title
changed between OS reads. The same PID and creation time were freshly
revalidated under the shared lifecycle claim, and `04b`/`05b` recorded the
owned blank lab. The refused record remains in `lead/`; it dispatched no
native DHCP operation.

The stage reestablished the exact disabled one-user physical `serverPool`
policy measured in episode 3. It then reapplied the compiled Server-PT
static-address action through E5 as a distinct product intervention. The
counted transport reported no acceptance-unknown dispatch. The product
action row reported `APPLIED`. Complete reads before and after showed the
same single `serverPool`: range `192.0.2.100-192.0.2.100`, network
`192.0.2.0/24`, gateway `192.0.2.1`, DNS `192.0.2.10`, capacity 1, and
singleton exclusions for `.1` and `.10`; the process stayed disabled.
`M-NATIVE-STABILITY` was **SUPPORTED_IN_SAMPLE** for retained physical
policy. E5's fire-and-forget JavaScript catches a native setter exception,
so this observation does not prove setter success or idempotence. It does
not measure restart/reload persistence, client service, or HTTP.

The run used 48 operations and proved fixture restoration. The maintained
retirement answered the exact owned save prompt and observed the PID exit.
The final census found zero Packet Tracer processes, pending mailbox files,
or campaign claim. The ledger closed episode 4 at 48 operations and
221.202394 seconds. Cumulative use is 163 operations and 1132.085253
seconds, leaving 8,837 ordinary operations and 12,667.914747 ordinary
seconds outside the protected reserve.
