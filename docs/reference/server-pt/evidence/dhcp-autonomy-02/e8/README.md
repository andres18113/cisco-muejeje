# SERVER-PT-DHCP-AUTONOMOUS-02: episode 8

This is the immutable primary archive of the first positive maintained product run on Packet Tracer 9.0.1.0858. `MANIFEST.sha256` hashes this README and every file in `lead/`, `record/`, and `store/`. The one-use Python scripts retain their bytes with `.txt` appended. `store/` is the cumulative campaign snapshot after the episode closed. Both records were copied, not moved; the originals stay where the campaign index cites them.

Executed commit `04c06adee4ac6a53299fc35556e8f31bac366ae5` (tree `6c5a6442f3ced8ba2f1552b9fd3fe99c37faccdd`), attempt `331fa0baf0974dfaa59efd69bd0590ac`, instance `8b0c54d7834c48c296fdc2bdc626da80`, file channel. Qualification record `q3-native-product-2026-09-26T04-06-23Z-dc9df810.json` has SHA-256 `127d0c0a7116b988b50afc5b58394350878368f3d134283a98867688938516e4`. Product run record `2026-09-26T04-06-23Z-dc9df810-product.json` has SHA-256 `6fa7a8dc0dbbbb3113ac90a61fdb5fe577459410d1b57aaa5ee309a035ad4c15`. Both are registered as campaign external sources.

The exact owned four-device, three-link fixture was created and verified. The maintained product A1-E6 path completed with overall status `verified`, stage and persisted stage `completed`, no refusal and no persistence error. E5 configured IE-2000 VLAN 10, two access ports, a static address on the server only and DHCP mode on PC1; E6 selected only HTTP enable, the native pool policy, DHCP enable and HTTP content. No explicit `dhcpRun`, ping, DNS, warm-up or client static address was dispatched.

The required `DHCP_LEASE` check for PC1 was VERIFIED by `native_dhcp_usable_state` at claim level `attributed_to_effective_server_pool`: `192.0.2.100/255.255.255.0`, MAC `0001.9677.3739`, effective pool `serverPool` for logical pool `Q3_DATA`, two stable samples of three, with PC2 (`0002.16CB.865E`) the checked inactive client. Its limitations are `autonomous_state_not_explicit_dhcpRun_causality` and `positive_row_does_not_establish_table_end`. The optional `dhcp_lease_attributed` check was unobservable (`lease_client_identity_invalid`).

Access readiness for switch `Q3-DEFAULT-ACCESS-SW-01`, VLAN 10, `FastEthernet1/1` and `FastEthernet1/3` was admitted through the simulation-time extension. The window saw both ports in `LIS` at 19.3 s and a read truncated by the 30-second deadline at 32.3 s; the extension (listening budget, 35 simulated seconds under an 80-second wall cap, offer 89 s) read both ports `FWD` at 53.6 s and stopped `converged`. The required `HTTP_FETCH` check was then VERIFIED as `independent_client_observation`: one request from PC1 to `http://192.0.2.10/`, the input URL, started at 1.782 s and read fresh content carrying marker `MCP-Q3-NATIVE-HTTP-READY` at 3.516 s.

The independent terminal reading was complete: PC1 DHCP on with `192.0.2.100/24`; PC2 DHCP off with `0.0.0.0/0.0.0.0`; `serverPool` row 0 exactly `192.0.2.100`, `0001.9677.3739`, `FastEthernet0`, and rows 1 to 3 throwing `invalid vector subscript`, which is not claimed as a table end; the separately named `Q3_DATA` pool absent; the server enabled with one pool `192.0.2.0/24`, gateway `192.0.2.1`, DNS `192.0.2.10`, window `.100`-`.100`, maximum one user and the two exclusions. `M-NATIVE-PRODUCT` and `M-NATIVE-PRODUCT-FINAL` are SUPPORTED_IN_SAMPLE for this disposable fixture and exact build. Fixture restoration was proven.

The launch was recorded on its first capture. Maintained retirement closed owned PID 25920 on its first attempt with basis `owned_qualification_restored`; the final census found zero Packet Tracer processes, no pending mailbox file and no campaign lock. Episode 8 used 93 qualification operations and closed at 330.315531 seconds. Cumulative use is 431 operations and 2349.124315 seconds, leaving 8,569 ordinary operations and 11,450.875685 ordinary seconds outside protected reserve. This is experimental validation under a private candidate capability snapshot, not default catalog promotion, renewal, explicit acquisition causality, native capacity beyond one user or independent product acceptance.
