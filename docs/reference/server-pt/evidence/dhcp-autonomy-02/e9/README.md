# SERVER-PT-DHCP-AUTONOMOUS-02: episode 9

This immutable archive contains the first two-selected-client maintained product
run on Packet Tracer 9.0.1.0858, file channel. Executed source `68680209eaaa5c88fd0f7cc30675232c645cc263`
(tree `c996b28b9744308b9833fdefdbf847b207fcae96`), attempt `22d8ccc6f02540f1842f42ad2d3fdf88`, instance
`55f5176a7f404f1c8066496732f86923`, profile `Q3-NATIVE-PRODUCT` version 2.

The requested unnamed state-only DHCP policy bound to physical `serverPool` on
`192.0.2.0/24`, gateway `192.0.2.1`, DNS/server `192.0.2.10`, capacity two,
window `.100`-`.101`, with `.1` and `.10` excluded. The maintained A1-E6
product record is VERIFIED and sealed. PC1 received `192.0.2.100` with MAC
`0002.4A40.D2C1`; PC2 received `192.0.2.101` with MAC `0040.0BAC.560C`.
Each result joined two stable fresh samples and a positive exact IP/MAC/port
row in `serverPool`; the group trace is retained once on the triggering client
and referenced by the other. Both first HTTP-by-IP requests to
`http://192.0.2.10/` returned the expected marker without ping, DNS, warm-up,
PortFast, clock acceleration, or client static-address injection.

`M-NATIVE-PRODUCT` and `M-NATIVE-PRODUCT-FINAL` are SUPPORTED_IN_SAMPLE for
this exact source, build, fixture, policy and channel. The qualification used
88 operations and 161.016 active seconds. The closed campaign ledger charged
405.774797 seconds for the whole episode; cumulative use is 519 operations
and 2754.899112 seconds. Fixture restoration was proven. Maintained retirement
exited owned PID 24784; final census showed zero Packet Tracer processes,
empty mailbox and no campaign lock. Explicit `dhcpRun` causality, renewal,
table-end proof, shifted policies, other networks/builds, public default
catalog registration and independent product acceptance are not claimed.

The qualification record SHA-256 is `288286ba175acb1848773120e8df2db3cc13a4c9eb424bdc8dc386185c74c081` and the product
record SHA-256 is `cc92eae7e34680db7dbeb510c3f9a83c2e33e88108bce9865cd33971549769de`. Both originals remain in their indexed
campaign locations. `store/` is the cumulative campaign snapshot after this
episode closed. `MANIFEST.sha256` pins README, lead, record and store bytes;
one-use Python scripts are preserved as `.py.txt` data.
