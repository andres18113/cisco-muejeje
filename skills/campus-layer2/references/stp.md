# STP reference

Choose root deterministically from explicit intent, role and stable tie-breakers. Verify current root, bridge, role and state. PortFast/BPDU Guard belongs only on semantically safe edge ports.

Failover evidence requires a verified forwarding baseline, controlled active-path failure, alternate forwarding state, recovered traffic and separate restoration evidence.
