interface:
  display_name: "packet-tracer-runtime"
  short_description: "Enterprise networking workflow for packet-tracer-runtime"
  default_prompt: "Use the packet-tracer-runtime workflow for this request."
