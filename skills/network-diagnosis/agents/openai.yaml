interface:
  display_name: "Network Diagnosis"
  short_description: "Explain observed network failures without mutating state"
  default_prompt: "Use $network-diagnosis to form and test evidence-supported causal hypotheses without changing the network or certifying readiness."
