# Configuration actions

Actions are closed typed values with stable IDs, source identities, capabilities, dependencies, desired-state semantics and verification expectations. Examples include VLAN, access, trunk, routed interface, DHCP and endpoint addressing actions.

Render IOS only at the backend boundary. A successful renderer or command submission is not direct observation.
