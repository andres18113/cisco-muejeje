interface:
  display_name: "enterprise-configuration"
  short_description: "Enterprise networking workflow for enterprise-configuration"
  default_prompt: "Use the enterprise-configuration workflow for this request."
