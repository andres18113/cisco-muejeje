# RIPv2

Read this reference only for typed RIPv2 intent, learned-route evidence, forwarding, or convergence work.

Use the current RIPv2 types in the shared control-plane model/compiler/runtime; do not fall back to an untyped routing recipe. Derive advertised networks, passive/transit intent, and any version-specific behavior from the approved plan and current source/tests.

Keep applied configuration, learned route, next hop, forwarding, withdrawal or alternate path, and restoration as separate evidence dimensions. Use fresh observations and preserve any parser or runtime limitation instead of borrowing OSPF or EIGRP semantics.
